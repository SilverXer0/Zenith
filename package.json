{
  "name": "zenith",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A calm, local-first personal manager.",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
